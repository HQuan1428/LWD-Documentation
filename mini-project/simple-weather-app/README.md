# A Simple Weather App witth API


## Fetch api 
```js
fetch(url)
    .then(Response => Response.json())
    .then(data => {/* Handle login for application */})
```
- To use an API, you need an API KEY to make fetch requests.
